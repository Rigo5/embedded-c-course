{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug firmware",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/firmware",
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "stopAtEntry": false,
            "externalConsole": false
        }
    ]
}